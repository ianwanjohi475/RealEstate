# Dependencies
node_modules/

# Firebase
.firebase/
firebase-debug.log
*-debug.log

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Env (root)
.env.local

# NOTE: server/.env IS committed at the owner's request so `git pull` works
# out of the box on a PRIVATE repo. Keep this repository private.

# Server deps
server/node_modules/
